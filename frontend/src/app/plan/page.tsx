import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function PlanPage() {
  return (
    <PagePlaceholder
      title="Plan Your Visit"
      description="Plan hub — routes to booking, enquiry, and experience discovery."
      specRef="Step 1 · /plan"
    />
  );
}
