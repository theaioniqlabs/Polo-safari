import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Account login gate before booking checkout. Wireframes reference /account/login; scaffold uses /login."
      specRef="Step 12 · Login gate"
    />
  );
}
