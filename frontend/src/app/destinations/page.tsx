import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function DestinationsPage() {
  return (
    <PagePlaceholder
      title="Destinations"
      description="Geographic destination catalogue — separate from experience pillars."
      specRef="Step 7 · /destinations"
    />
  );
}
